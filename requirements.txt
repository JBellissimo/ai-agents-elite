anthropic>=0.40.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
discord.py>=2.4.0

# Phase 0: Telegram bot (replaces Discord in Phase 1)
python-telegram-bot>=20.0

# Phase 2: Cron scheduling for 5am brief, nightly memory, etc.
apscheduler>=3.10.0

# Phase 2: Write to Obsidian vault via Git push
gitpython>=3.1.0
